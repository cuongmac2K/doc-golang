package main

import "fmt"

func main() {
	a := []int{2, 3, 5}
	k := 7
	fmt.Println(combinationSum(a, k))

}
func combinationSum(candidates []int, target int) (arr [][]int) {
	for i := 0; i < len(arr); i++ {

	}
	return
}

func checkTrue(arr []int, k int) (check bool) {
	n := len(arr)
	for i := 0; i < n; i++ {
	s:
	}
	return
}
